# Assets Compass

Tracker for financial data.

<p align="center">
  <img width="45%" src="./assets/logo.png" />
</p>

## Run Instructions

### Configuration

Make sure the root directory contains file named `.env` with all variables initialized:

```shell
DB_USER=<set_username>
DB_PASS=<set_password>
...
```

### Services

```shell
docker-compose up
```

#### DB Dashboard

GUI for DBMS is available at port `5433`. To connect to the database,
fill in all the details from `.env`, and use `host.docker.internal` as the host.

### Migrations

```shell
alembic upgrade head
```

### Celery

To start the worker instance:

```shell
celery -A src.tasks.tasks:celery worker --loglevel=INFO --pool=solo
```

To enable a web interface for background tasks:

```shell
celery -A src.tasks.tasks:celery flower
```

By default, the dashboard is accessible on `localhost:5555`.

### Web Application

```shell
poetry shell
uvicorn src.main:app --reload
```

## Notes

- By default, the application is available at port `8000`
- See autogenerated docs at `/redoc` endpoint
- When calling `/login` endpoint to authorize, use **email** as the **username**
- Currently, `pytest .` doesn't exit successfully: `prepare_database` fixture fails to create tables
  - most likely, it is caused by imperfect pytest-asyncio integration. Won't be focusing on fixing this much.
